###> symfony/framework-bundle ###
# Dev APP_SECRET — set in your local .env.local if you need to override.
# Symfony auto-generates one in production via composer install if missing.
APP_SECRET=
###< symfony/framework-bundle ###
